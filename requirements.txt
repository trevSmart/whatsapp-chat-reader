# WhatsApp Chat Reader - Dependencies

# Core dependencies for the progressive server
Flask>=2.0.0
Flask-CORS>=3.0.0

# Python standard library modules used:
# - re (regular expressions)
# - os (operating system interface)
# - datetime (date and time handling)
# - typing (type hints)
# - dataclasses (data classes)
# - base64 (base64 encoding)
# - mimetypes (MIME type detection)
# - pathlib (path handling)
# - argparse (command line argument parsing)
# - webbrowser (opening web pages)
# - json (JSON handling)
# - sys (system-specific parameters)

# Optional dependencies for enhanced features:
# - Pillow (PIL) for advanced image processing
# - python-dateutil for better date parsing
# - jinja2 for more advanced HTML templating
